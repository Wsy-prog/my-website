# Neon 数据库连接字符串（必填，代码中读取 PG_URL）
PG_URL=postgresql://user:password@host/neondb?sslmode=require

# 管理员密码（必填，部署时在 Vercel 环境变量中设置）
ADMIN_PASSWORD=你想要的密码

# JWT 签名密钥（必填，使用随机字符串，部署时在 Vercel 环境变量中设置）
JWT_SECRET=替换为随机字符串
